# Path to the SQLite database file. The parent directory is created on startup.
DATABASE_PATH=./data/easy-order.sqlite

# Host/port the production server (bun ./build/index.js) listens on.
HOST=0.0.0.0
PORT=3000

# Public origin, used by SvelteKit for CSRF origin checks behind a proxy.
ORIGIN=http://localhost:3000
