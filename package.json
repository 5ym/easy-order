{
  "name": "easy-order",
  "version": "2.0.0",
  "devDependencies": {
    "@sveltejs/adapter-node": "5.5.7",
    "@sveltejs/kit": "2.70.3",
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "@tailwindcss/vite": "4.3.3",
    "@types/bun": "1.3.14",
    "@typescript/native": "npm:typescript@7.0.2",
    "daisyui": "5.7.19",
    "svelte": "5.56.9",
    "svelte-check": "4.7.6",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3",
    "vite": "8.2.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "private": true,
  "scripts": {
    "dev": "bun --bun vite dev",
    "build": "bun --bun vite build",
    "preview": "bun --bun vite preview",
    "start": "bun ./build/index.js",
    "prepare": "svelte-kit sync",
    "check": "svelte-kit sync && svelte-check --tsgo --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsgo --tsconfig ./tsconfig.json --watch",
    "test": "bun test",
    "db:migrate": "bun run src/lib/server/db.ts",
    "test:e2e": "bun run build && bash scripts/smoke.sh"
  },
  "type": "module"
}
